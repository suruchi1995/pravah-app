# Copy to .env (backend) or set in Render dashboard. NEVER commit the real .env.
DATABASE_URL=postgresql://user:pass@host/dbname
ANTHROPIC_API_KEY=sk-ant-xxxxxxxx   # set ONLY as a server env var
DEFAULT_TENANT=apex
